# TODO

## 保留: 予測取得失敗時の 1 時間前フォールバック（UI）

- **背景**: andersan-api はデータが取れない場合に 404 / 503 を返し、1 時間前のデータを代わりに返すことはない。
- **方針（将来）**: 取得失敗時に「1 時間前の時刻で予測 API を再試行し、代わりに表示するか」は **UI 側の判断**で実装する余地がある。
- **状態**: 動作状況の確認のため **いまは実装しない（保留）**。
- **実装時の論点のメモ**:
  - 現在時刻の `fetchPredict` / `fetchObserve` が失敗したときの分岐（例: 1 時間前の ISO で再フェッチ）。
  - グラフの横軸・注記で **実効の基準時刻** をユーザーに明示する（誤解防止）。
  - `src/retrieve.js` に未使用の `getOneHourAgo` があるので、実装時に流用または整理する。
